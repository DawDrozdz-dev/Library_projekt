                                            1. Klasa: GetStringValue
1.1 Opis:
Klasa `GetStringValue` implementuje interfejs `IDataManager` i służy do pobierania danych wejściowych od użytkownika.

1.2 Metody:
- `public string? GetData()`: Pobiera dane wejściowe od użytkownika za pomocą `Console.ReadLine()` i zwraca je jako `string`.

1.3 Przykład użycia:
IDataManager dataManager = new GetStringValue();
string? userInput = dataManager.GetData();
Console.WriteLine($"User input: {userInput}");

                                            2. Interfejs: IDataManager
2.1 Opis:
Interfejs `IDataManager` definiuje metodę do pobierania danych.

 2.2 Metody:
- `string? GetData()`: Pobiera dane i zwraca je jako `string`.
